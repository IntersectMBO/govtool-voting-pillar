type Props = {
    isChecked?: boolean;
    text: string;
    variant?: 'blueThin' | 'blue';
};
/**
 * CopyButton component.
 *
 * @param isChecked - Indicates whether the button is checked.
 * @param text - The text to be copied to the clipboard.
 * @param variant - The variant of the button.
 * @returns The CopyButton component.
 */
export declare const CopyButton: ({ isChecked, text, variant }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
