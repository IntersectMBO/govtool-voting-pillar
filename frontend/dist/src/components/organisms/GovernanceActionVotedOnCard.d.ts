import { VotedProposal } from '../../models';
type Props = {
    votedProposal: VotedProposal;
    inProgress?: boolean;
};
export declare const GovernanceActionVotedOnCard: ({ votedProposal, inProgress, }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
